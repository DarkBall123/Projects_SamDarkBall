## Operators

- Source: https://community.bistudio.com/wiki/Operators
- Revision: 363637
- Updated: 2024-04-09T03:49:39Z

{{TOC|side}}
**Operators** are the base commands each programming language is built on. They provide ability to perform basic mathematical and logical operations.

### Requirements

To understand this article, you should read the following articles:

- Variables

### Terms

- Operand
- An operand is any value given to an operator.

- Expression
- An expression is basically any code that returns a value. Read expression for more information.

- Unary Operator
- An unary operator is an operator that requires only one operand.
- 
- Unary operation:
- <code style="display: block">*operator* expression</code>

- Binary Operator
- A binary operator is an operator that requires two operands.
- 
- Binary operation:
- <code style="display: block">expression *operator* expression</code>

### Operators

#### Assignment Operators

Assignment operators are used to assign values to a variable. OFP's scripting language provides only one assignment operator.

**Assignment:**
 identifier = expression

**Example 1:**

```sqf
a = b
```

You might think that this operator compares `a` and `b`, but that is not the case. {{hl|{{=}}}} simply sets the left value to be the right one. Other assignment operators like `c= +=`, `c= -=` that can be found in other programming languages do not exist in SQF/SQS.

**Example 2:**

```sqf
a = b * c
```

#### Arithmetic Operators

Remember arithmetic operations from school? These work just the same way.
All operands of arithmetic operations must be Numbers. Arithmetic operations always return a Number.

```mediawiki-table
{| class="wikitable"
|+ Unary arithmetic operators (in order of precedence)
! Operator !! Name !! Example
|-
| - || Negation ||

```sqf
-a
```

|-
| + || Duplication ||

```sqf
+a
```

|-
| ( || Bracket  ||

```sqf
(expression)
```

|}
```

```mediawiki-table
{| class="wikitable"
|+ Binary arithmetic operators
! Operator !! Name !! Example
|-
| + || Addition ||

```sqf
a + b
```

|-
| - || Subtraction ||

```sqf
a - b
```

|-
| * || Multiplication ||

```sqf
a * b
```

|-
| / || Division ||

```sqf
a / b
```

|-
| % || Modulo ||

```sqf
a % b
```

|-
| mod || Modulo ||

```sqf
a mod b
```

|-
| ^ || Raise to the power of ||

```sqf
a ^ b
```

|}
```

See also Math Commands.

#### Logical Operators

Logical operators evaluate Boolean values. All operands of logical operations are Booleans.
A logical operation always returns a Boolean.

```mediawiki-table
{| class="wikitable"
|+ Unary logical operators
! Operator !! Name !! Example
|-
| ! || Not ||

```sqf
!a
```

|-
| not || Not ||

```sqf
not a
```

|}
```

The `Not`-operator always returns the inverse Boolean value. If a Boolean `a` is `true`, `!a` returns `false` and vice versa.

```mediawiki-table
{| class="wikitable"
|+ Binary logical operators
! Operator !! Name !! Example
|-
| && || And ||

```sqf
a && b
```

|-
| and || And ||

```sqf
a and b
```

|-
| || || Or ||

```sqf
a || b
```

|-
| or || Or ||

```sqf
a or b
```

|}
```

- `And` only returns `true` if *both* operands are `true`
- `Or` returns `true` if *one or both* operands are `true`

There is no Xor, Nor and Nand operator. Those can be simulated using the basic operators though:

```mediawiki-table
{| class="wikitable"
|+ Combined logical operators
! Name !! Combination
|-
| Xor ||

```sqf
((a || b) && !(a && b))
```

|-
| Nor ||

```sqf
!(a || b)
```

|-
| Nand ||

```sqf
!(a && b)
```

|}
```

- `Xor` returns `true` if *exactly one* of both values is `true`
- `Nor` returns `true` if *none* of both values is `true`
- `Nand` returns `true` if *not both* values are `true` at the same time

#### Comparison Operators

Comparison operators compare two values. Operands of comparisons may be of type Number, Side, String, Object, Group, Structured Text, Config, Display or Control for `c= ==` and `c= !=`, and Number for `c= < > >= <=`.
Comparisons always return a Boolean: `true` if the comparison matches, `false` if not.

```mediawiki-table
{| class="wikitable"
|+ Comparison operators
! Operator !! Name !! Example
|-
| == || Equal ||

```sqf
a == b
```

|-
| != || Not equal ||

```sqf
a != b
```

|-
| < || Less than ||

```sqf
a < b
```

|-
| > || Greater than ||

```sqf
a > b
```

|-
| <= || Less or equal ||

```sqf
a <= b
```

|-
| >= || Greater or equal ||

```sqf
a >= b
```

|}
```

{{Feature|informative|2=
{{{!}} class="wikitable" style="float: right"
! invalid before Arma 3 v2.00
! valid in all versions
{{!}}-
{{!}}

```sqf
a == true
```

{{!}}

```sqf
a
```

{{!}}-
{{!}}

```sqf
a == false
```

{{!}}

```sqf
!a
```

{{!}}}
You could not compare Boolean values with {{=}}{{=}} before **Arma 3 v2.00** (in Arma 3 before that, use isEqualTo).
Comparing a Boolean value with `true` is the same as the value itself.
Comparing a Boolean value with `false` is the same as the *inverse* value.
}}

#### Array Operators

The scripting language offers own operators to deal with arrays. All operands, of course, have to be of type Array.
The return value of an array operation is an Array.

```mediawiki-table
{| class="wikitable"
|+ Unary array operators
! Operator !! Name !! Example
|-
| + || Copy ||

```sqf
+myArray
```

|}
```

Normally arrays are assigned *by reference*. That means, if you assign array `a` to array `b` and change `a` afterwards, also `b` is changed. Use the copy operator to avoid this otherwise useful feature.

**Example 1:**

```sqf
_arrayA = [1,2];
_arrayB = _arrayA;
_arrayA set [0,5];

// _arrayA => [5,2]
// _arrayB => [5,2]
```

**Example 2:**

```sqf
_arrayA = [1,2];
_arrayB = +_arrayA;
_arrayA set [0,5];

// _arrayA => [5,2]
// _arrayB => [1,2]
```

```mediawiki-table
{| class="wikitable"
|+ Binary array operators
! Operator !! Name !! Example
|-
| + || Concatenation ||

```sqf
myArray1 + myArray2
```

|-
| - || Removal ||

```sqf
myArray1 - myArray2
```

|}
```

- `+` adds the second operand on the end of the first operand
- `-` removes all elements of the second operand from the first operand

**Example 1:**

```sqf
_arrayA = [1,2];
_arrayB = [3,2,4];

_arrayC = _arrayA + _arrayB;

// _arrayC => [1,2,3,2,4]
```

**Example 2:**

```sqf
_arrayA = [1,2,3,2,4];
_arrayB = [2,3];

_arrayC = _arrayA - _arrayB;

// _arrayC => [1,4]
```

#### String Operators

The scripting language offers one single string operator to concatenate strings. Both operands must be Strings.
The return value of a string operation is a String.

```mediawiki-table
{| class="wikitable"
|+ Binary string operators
! Operator !! Name !! Example
|-
| + || Concatenation ||

```sqf
myString1 + myString2
```

|}
```

- `+` adds the second operand on the end of the first operand

**Example:**

```sqf
_stringA = "Hello ";
_stringB = "World!";

_stringC = _stringA + _stringB;

// _stringC => "Hello World!"
```

#### Config Operators

```mediawiki-table
{| class="wikitable"
|+ Config operators
! Operator !! Name !! Example
|-
| += || Append Array ||

```
myProperty[] += {element_1, element_2};
```

|}
```

**Example:**

```
class B_Soldier_F;
class MySoldier: B_Soldier_F
{
	RespawnItems[] += {"FirstAidKit"}; //Adds additional FAK
};

/*
	Original RespawnItems[] array in config viewer:
	RespawnItems[] = {"FirstAidKit"};

	Modified RespawnItems[] array in config viewer:
	RespawnItems[] = {"FirstAidKit","FirstAidKit"};
*/
```

### Order of Precedence

{{:Order of Precedence}}

### See Also

- SQF Syntax
- SQS Syntax

Category: Syntax
