## Arma 2: ULB Module

- Source: https://community.bistudio.com/wiki/Arma_2:_ULB_Module
- Revision: 377234
- Updated: 2026-01-02T03:15:45Z

{{TOC|side}}
### Usage

#### Usage

1. Switch to ULB using **"ULB"** item in action menu
1. {{Controls|LMB}} to 'fire' the laser
1. When conditions are good (HUD in top left corner - AMMO > 0, ACC > 0), press {{Controls|Ctrl|LMB}} or action from menu to fire a missile from player's vehicle

#### Default Controls

- "ULB" option in action menu - switch to ULB view
- {{Controls|NumEnter}} - exit the ULB view
- {{Controls|Ctrl|LMB}} - fire weapon from original helicopter
- {{Controls|Q}} - Up
- {{Controls|Y}} - Down
- {{Controls|X}} - Turn left
- {{Controls|C}} - Turn right
- {{Controls|B}} - Mark position

- {{Controls|LMB}} in map - place new waypoint
- {{Controls|Ctrl|LMB}} in map - add new waypoint to currently existing ones

### Startup

#### Editor Setup

File:ULB.jpg
1. **ULB manager** - module manager, can be found in *Modules (F7) -> ULB*.
1. **ULB** - unit with parent

```
isULB = 1
```

. Multiple ULBs can be assigned to one manager, the latest has the greatest priority.
1. **Units with access** - any units without

```
isUAV = 1
```

 config param. They will have 'ULB' action in menu.

#### Optional Parameters

Set variables to ULB manager with desired values:

- **name = <string>;** - name of the ULB (displayed in action menu)
- : Example:

```sqf
BIS_heli_uav_0 setVariable ["name", "AH-6X"];
```

- : Default value: empty string

- **unitList = <array>;** - List of units with access to ULB. Same as synchronization to non-ULB units.
- : Example:

```sqf
BIS_heli_uav_0 setVariable ["unitList", [hco_west_1, hco_west_2]];
```

- : Default value: units synchronised in editor

- **UAVList = <array>;** - List of ULBs. Same as synchronization to ULB units.
- : Example:

```sqf
BIS_heli_uav_0 setVariable ["UAVList", [ULB_1, ULB_2]];
```

- : Default value: ULBs synchronised in editor

{{GameCategory|arma2|Editor Modules}}
