## actionKeysNames

- Source: https://community.bistudio.com/wiki/actionKeysNames
- Revision: 364173
- Updated: 2024-05-05T10:46:45Z

- Availability: ofpe 1.00, arma1 1.00, arma2 1.00, arma2oa 1.50, tkoh 1.00, arma3 0.50
- Groups: Interaction

### Description

Returns a list of button names assigned to the given user action.
Action names can be found in config class `ControllerSchemes` or user action names.

### Syntax

```sqf
actionKeysNames  userAction
```

```sqf
actionKeysNames [userAction, maxKeys, inputDevicePriority]
```

### Parameters

- userAction: String
- userAction: String
- maxKeys: Number - maximum number of keys to return
- inputDevicePriority: String - result sorted by inputDevicePriority with values:
* `"Mouse"`
* `"Keyboard"`
* `"Stick"`
* `"Gamepad"`
* `"Tracker"`
* `"Combo"` (mouse and keyboard combinations)
* {{sic|`"Controler"`}} (any controller other than mouse or keyboard)
Other values are considered as: 
* `"Unsorted"`

### Return Value

- String
- String

### Examples

#### Example 1

```sqf
_list = actionKeysNames "ReloadMagazine"; // """R"" or ""XBox X"""
```

### See Also

actionKeys actionKeysImages actionKeysNamesArray actionName inputAction
}}

{{Note
|user= WGL.Q
|timestamp= 20100120201400
|text= The return type is a stacked string. A string inside a string. To compare use first single and inside double quotes:

```sqf
if (actionKeysNames "User1" == '"W"') then { /* ... */ };
```
