name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: SQF validate
        run: python3 tools/sqf_validator.py --config tools/ci_config.json

      - name: Config style check
        run: python3 tools/config_style_checker.py --config tools/ci_config.json
